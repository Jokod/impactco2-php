grumphp:
    tasks:
        file_size:
            max_size: 5M
        phpcsfixer:
            config: .php-cs-fixer.dist.php
        phpunit: ~
    git_hook_variables:
        PATH: '~/.composer/vendor/bin:$PATH'
    fixer:
        enabled: false
